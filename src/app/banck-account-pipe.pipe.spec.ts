import { BanckAccountPipePipe } from './banck-account-pipe.pipe';

describe('BanckAccountPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BanckAccountPipePipe();
    expect(pipe).toBeTruthy();
  });
});
